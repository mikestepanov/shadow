#!/run/current-system/sw/bin/bash
# Legacy convenience wrapper: install only sync-repos timer via the new timer manager.

set -euo pipefail

SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"

exec "$SCRIPT_DIR/timers-install" sync-repos
