# 2026-02-08 Session Notes

## Jules PR Review Workflow Setup

### What We Built
- **Cron job** (`eb0530be-6d4a-4545-9ab5-27121696af00`): "Jules PR Reviewer"
  - Runs every 30 minutes
  - Uses `google-antigravity/gemini-3-pro-high` (saves Claude tokens)
  - Reviews PRs with Jules branch patterns (bolt/, palette/, sentinel/)
  - Applies CodeRabbit fixes, resolves conflicts, squash merges

### Infrastructure
- **Separate clone**: `~/Desktop/nixelo-agent` — for automated PR work (no GitButler)
- **Main repo**: `~/Desktop/nixelo` — interactive dev, removed GitButler today
- GitButler removed from nixelo: deleted `.git/gitbutler/` and `gitbutler/workspace` branch

### NixOS Paths (critical for sub-agents)
```
export PATH="/nix/store/dh2yzrnp5raifh3knbphhljrsjqkcklr-git-2.51.2/bin:/nix/store/7bmqbqbpaxhf7k29m1v820mr3xl5mb52-coreutils-9.8/bin:/nix/store/c4091kqz1rw8n6vmygbvvwpr00ghdmks-gh-2.83.2/bin:/run/current-system/sw/bin:$PATH"
```

### PRs Reviewed & Merged Today
- **#175** (Bolt: Optimize listSelectableIssues) — fixed Map import (aliased as MapIcon)
- **#173** (Sentinel: Fix bulk update deleted issues) — fixed test assertions
- **#174** (Palette: Link input accessibility) — removed unused variable, auto-merge enabled

### Lessons Learned
1. Sub-agent burned 116k Claude tokens exploring NixOS paths — must include paths in cron job prompt
2. Sub-agents need explicit model specification or they default to Claude
3. GitButler conflicts with `git checkout` — use separate clone for automation
4. Conflict resolution: `git checkout --ours . && git add . && GIT_EDITOR=true git rebase --continue`

### Cron Job Config
- Model: `google-antigravity/gemini-3-pro-high`
- Workdir: `~/Desktop/nixelo-agent`
- Includes: PATH setup, conflict resolution steps, squash merge with --admin
