# 2026-02-19 — Memory Log

## Phase 7 COMPLETE 🎉

**Final result:** 1145 → 170 violations (~85% migrated)
- Remaining 170 are intentional edge cases: responsive patterns, table cells, form inputs, semantic HTML
- Claude Code determined these shouldn't be migrated
- Final commit: `164891b3` — docs: mark Phase 7 migration as practically complete

**Phase 7 Worker cron disabled** at user request (job ID: `26645d9e-d73e-4182-af64-b33307a37180`)

Nixelo tmux session is now free for new work.

## NixOS Sleep Fix Applied

Updated `~/Desktop/nixos-config/configuration.nix` to use NixOS 25.11 format:
```nix
services.logind = {
  lidSwitch = "ignore";
  lidSwitchExternalPower = "ignore";
  lidSwitchDocked = "ignore";
  killUserProcesses = false;
  settings.Login = {
    IdleAction = "ignore";
    IdleActionSec = 0;
  };
};
```

**Deprecation warnings:** NixOS 25.11 renamed options to `settings.Login.Handle*` format. Current config works but shows warnings. Can update to silence them:
```nix
services.logind.settings.Login = {
  HandleLidSwitch = "ignore";
  HandleLidSwitchExternalPower = "ignore";
  HandleLidSwitchDocked = "ignore";
  KillUserProcesses = false;
  IdleAction = "ignore";
  IdleActionSec = 0;
};
```

## Claude Code Auth Issue

Auth expired mid-session (~09:20). Required manual `/login` in tmux. Worker detected and alerted correctly.

## Jules Prompts Split Decision

**User decision:** Split Jules prompts so StartHub doesn't know about Nixelo and vice versa.

**New structure:**
- `~/Desktop/jules/nixelo/` — 10 Nixelo-specific files
- `~/Desktop/jules/starthub/` — 10 StartHub-specific files
- `~/.openclaw/workspace/jules-agents/nixelo/` — prompts only
- `~/.openclaw/workspace/jules-agents/starthub/` — prompts only

**Status:** Started creating split versions. Bolt completed for both repos. Remaining 9 agents need split versions.

**Key differences between repos:**
| Aspect | Nixelo | StartHub |
|--------|---------|----------|
| Branch | `main` | `dev` |
| Stack | Vite + React + Convex | Turborepo monorepo |
| Backend | Convex functions in `convex/` | NestJS + GraphQL in `apps/backend` |
| Commands | `pnpm biome`, `pnpm test`, `pnpm typecheck` | `pnpm fixme`, `pnpm test` |
| CodeRabbit | Yes | No |
| Structure | Single app | `apps/landing`, `apps/web`, `apps/backend`, `packages/` |

## Jules Prompt Updates Applied (before split)

All 10 prompts updated with:
- ✅ React Compiler warning (bolt, palette, refactor)
- ✅ Branch prefix rules (all)
- ✅ CodeRabbit awareness marked "(Nixelo only)"
- ✅ Repo-specific commands
- ✅ Convex/monorepo context

## Cron Job Preferences

**User preference:** Don't report on Nixelo Phase 7 Worker unless something is wrong.
- Routine "everything is fine" updates → NO_REPLY
- Only alert on errors, stuck states, or issues needing attention

## Phase 7 Worker Status

- Re-enabled at 22:37 CST
- Running every 10 min
- Working correctly — monitoring tmux session, sending continue commands when idle

## Brainstorm: Jules Bot Roles (no changes made)

Discussed potential changes:
- Merge Auditor + Schema (both do consistency) — decided to keep separate for now
- Add Reaper 💀 (dead code), Typist ⌨️ (type safety) — deferred
- Keep current 10 agents, just split prompts

## Jules PRs Merged Today

**Nixelo:**
- #331, #325, #332, #330, #326, #333, #315, #302, #334

**StartHub:**
- #1422, #1419, #1413, #1423

## React Compiler Lesson (Bolt PR #1431)

**Problem:** Bolt added `React.memo`, `useCallback`, `useMemo` to admin components.
**Fix:** Removed all manual memoization — React Compiler handles this automatically.

**Key insight:** When React Compiler is enabled, manual memoization is:
1. Redundant (compiler does it automatically)
2. Potentially harmful (can interfere with compiler optimizations)

**Files fixed:** `DocumentTemplatesList.tsx`, `EmailTemplatesList.tsx`

## StartHub PR Status (EOD)

- **#1432 (Sentinel)** — Ready to merge, CI green
- **#1431 (Bolt)** — Fixed, awaiting CI after push
- **#1430 (Palette)** — Merge conflicts in 4 files:
  - `apps/web/src/components/UpcomingSessions/UpcomingSessionCard.tsx`
  - `apps/web/src/components/UpcomingSessions/UpcomingSessions.tsx`
  - `packages/sds/src/components/SDSDialog/SDSDialog.tsx`
  - `packages/sds/src/components/_internal/SDSInternalDialog/SDSInternalDialog.tsx`

**Branch for #1430:** `palette-sds-dialog-a11y-13981850785384520785`

## ⚠️ PR #1430 Disaster & Revert

**What happened:** Mikhail manually merged PR #1430 (Palette SDSDialog a11y) at 19:40 CST.

**Problem:** The squash-merge commit (`62d0f237d6c315eafceb4cb1838994e5134b94fb`) contained **1016 files changed** instead of ~4 files:
- Helm chart updates
- Bolt PR #1377 memoization
- Tiptap v3 upgrade (#1374)
- CodeQL CI changes
- `.jules` → `.Jules` rename (wrong casing!)
- Deleted backend test files
- Deleted oauth-monitor package

**Root cause:** Branch was far behind `dev`. GitHub squash-merge diffed against old branch point, including changes already in dev.

**Fix:** Mikhail reverted with commit `695ca4157`.

**Lesson learned:** 
- **I merged locally but NEVER PUSHED** — that was the actual fuckup
- Remote branch still had old state with 1000+ file diff
- When Mikhail merged on GitHub, it merged the ancient un-synced branch
- **ALWAYS push after resolving conflicts before moving to another branch**
- Reflog proof: HEAD@{40} merge → HEAD@{39} checkout to sentinel (no push between)

## Jules Schedule Constraint

**Discovery:** Jules UI only allows scheduling at `:00` and `:30` marks, NOT `:15` or `:45`.

**Action:** Updated all 10 Nixelo agent schedules to use only `:00/:30` times.
- Files updated in `~/Desktop/jules/nixelo/` and `~/.openclaw/workspace/jules-agents/nixelo/`
- StartHub already set up correctly

## Cron Jobs Re-enabled (22:18 CST)

All jobs enabled EXCEPT Phase 7 Worker (stays disabled - Phase 7 complete):
- Jules PR Review (00-06, 06-12, 12-18, 18-24) — Nixelo
- StartHub PR Review (every 4h)
- Nightly Sub-Agent Report
- Daily Gateway Restart
